package com.revature.models;

public enum Role {
    //enum = enumerated type
        // used to define collection of CONSTANT values - when we want only specific options for user
        // constructors of enums are always private

    ADMIN, DEV, TRAINER, QC_ANALYST, BLDNG_MNGR, SITE_MNGR;

    // can have static methods
    static void thing() {

    }
}
